
import hashlib import sqlite3 import csv import os from tkinter import * from tkinter import messagebox from tkinter import ttk # ========================= # BASE DE DATOS .DB # ========================= conexion = sqlite3.connect("usuarios.db") cursor = conexion.cursor() cursor.execute(""" CREATE TABLE IF NOT EXISTS usuarios ( id INTEGER PRIMARY KEY AUTOINCREMENT, usuario TEXT, password TEXT ) """) conexion.commit() # ========================= # FUNCIONES # ========================= def encriptar_password(password): return hashlib.sha256(password.encode()).hexdigest() # ========================= # REGISTRO # ========================= def registrar_usuario(): usuario = entry_usuario.get() password = entry_password.get() if usuario == "" or password == "": messagebox.showerror( "Error", "Complete todos los campos" ) return password_encriptada = encriptar_password(password) cursor.execute( "INSERT INTO usuarios (usuario, password) VALUES (?, ?)", (usuario, password_encriptada) ) conexion.commit() messagebox.showinfo( "Registro", "Usuario registrado correctamente" ) # ========================= # LOGIN # ========================= def iniciar_sesion(): usuario = entry_usuario.get() password = entry_password.get() password_encriptada = encriptar_password(password) cursor.execute( "SELECT * FROM usuarios WHERE usuario=? AND password=?", (usuario, password_encriptada) ) usuario_encontrado = cursor.fetchone() if usuario_encontrado: messagebox.showinfo( "Bienvenido", "Inicio de sesión exitoso" ) abrir_sistema() else: messagebox.showerror( "Error", "Usuario o contraseña incorrectos" ) # ========================= # GUARDAR CSV # ========================= def guardar_csv(estudio, sueno, estado): nombre_archivo = "habitos.csv" archivo = open( nombre_archivo, mode="a", newline="" ) escritor = csv.writer(archivo) escritor.writerow([ estudio, sueno, estado ]) archivo.close() # ABRIR EXCEL os.startfile(nombre_archivo) # ========================= # REPORTE GRÁFICO # ========================= def mostrar_grafico(estudio, sueno): ventana_grafica = Toplevel() ventana_grafica.title("Reporte de Hábitos") ventana_grafica.geometry("500x420") ventana_grafica.config(bg="#F4F6F7") Label( ventana_grafica, text="REPORTE FINAL", font=("Arial", 18, "bold"), bg="#F4F6F7", fg="#2C3E50" ).pack(pady=20) # ========================= # ESTUDIO # ========================= Label( ventana_grafica, text=f"Horas de estudio: {estudio}", font=("Arial", 12), bg="#F4F6F7" ).pack() barra_estudio = ttk.Progressbar( ventana_grafica, orient="horizontal", length=350, mode="determinate" ) barra_estudio.pack(pady=10) barra_estudio["value"] = estudio * 10 # ========================= # SUEÑO # ========================= Label( ventana_grafica, text=f"Horas de sueño: {sueno}", font=("Arial", 12), bg="#F4F6F7" ).pack() barra_sueno = ttk.Progressbar( ventana_grafica, orient="horizontal", length=350, mode="determinate" ) barra_sueno.pack(pady=10) barra_sueno["value"] = sueno * 10 # ========================= # MENSAJE FINAL # ========================= if estudio >= 4 and sueno >= 7: mensaje = "Excelente trabajo.\nTus hábitos son saludables." else: mensaje = "Debes mejorar tus hábitos." Label( ventana_grafica, text=mensaje, font=("Arial", 12, "bold"), fg="#1E8449", bg="#F4F6F7" ).pack(pady=30) # ========================= # SISTEMA PRINCIPAL # ========================= def abrir_sistema(): ventana_sistema = Toplevel() ventana_sistema.title( "Sistema Inteligente de Hábitos" ) ventana_sistema.geometry("500x500") ventana_sistema.config(bg="#ECF0F1") Label( ventana_sistema, text="CONTROL DE HÁBITOS", font=("Arial", 18, "bold"), bg="#ECF0F1", fg="#2C3E50" ).pack(pady=20) # ========================= # FORMULARIOS # ========================= Label( ventana_sistema, text="Horas de estudio", font=("Arial", 12), bg="#ECF0F1" ).pack() entry_estudio = Entry( ventana_sistema, font=("Arial", 12), width=25, justify="center" ) entry_estudio.pack(pady=10) Label( ventana_sistema, text="Horas de sueño", font=("Arial", 12), bg="#ECF0F1" ).pack() entry_sueno = Entry( ventana_sistema, font=("Arial", 12), width=25, justify="center" ) entry_sueno.pack(pady=10) resultado_label = Label( ventana_sistema, text="", font=("Arial", 11), bg="#ECF0F1", fg="#34495E" ) resultado_label.pack(pady=20) # ========================= # CALCULAR # ========================= def calcular(): try: estudio = int(entry_estudio.get()) sueno = int(entry_sueno.get()) promedio = (estudio + sueno) // 2 total = estudio + sueno multiplicacion = estudio * sueno # EVALUACIÓN if estudio >= 4 and sueno >= 7: estado = "Saludable" else: estado = "Mejorar" resultado = f""" Promedio: {promedio} Total de horas: {total} Multiplicación: {multiplicacion} Estado: {estado} """ resultado_label.config(text=resultado) # GUARDAR CSV guardar_csv( estudio, sueno, estado ) # MOSTRAR REPORTE mostrar_grafico( estudio, sueno ) except: messagebox.showerror( "Error", "Ingrese solamente números enteros" ) Button( ventana_sistema, text="Generar Reporte", command=calcular, bg="#3498DB", fg="white", font=("Arial", 12, "bold"), width=20 ).pack(pady=20) # ========================= # VENTANA PRINCIPAL # ========================= ventana = Tk() ventana.title("Sistema Inteligente") ventana.geometry("450x380") ventana.config(bg="#D6EAF8") Label( ventana, text="SISTEMA DE HÁBITOS", font=("Arial", 20, "bold"), bg="#D6EAF8", fg="#1B4F72" ).pack(pady=25) # ========================= # FORMULARIOS LOGIN # ========================= Label( ventana, text="Usuario", font=("Arial", 12), bg="#D6EAF8" ).pack() entry_usuario = Entry( ventana, font=("Arial", 12), width=28, justify="center" ) entry_usuario.pack(pady=10) Label( ventana, text="Contraseña", font=("Arial", 12), bg="#D6EAF8" ).pack() entry_password = Entry( ventana, show="*", font=("Arial", 12), width=28, justify="center" ) entry_password.pack(pady=10) # ========================= # BOTONES # ========================= Button( ventana, text="Registrar", command=registrar_usuario, bg="#2E86C1", fg="white", font=("Arial", 11, "bold"), width=18 ).pack(pady=10) Button( ventana, text="Iniciar Sesión", command=iniciar_sesion, bg="#28B463", fg="white", font=("Arial", 11, "bold"), width=18 ).pack(pady=10) # ========================= # EJECUTAR # ========================= ventana.mainloop() conexion.close()